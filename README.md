# Sample Node.js Repo

Minimal Node.js app for testing.

Run locally:

```bash
cd samplenodejsrepo
npm install
npm start
```

Then open http://localhost:3000 in your browser or `curl http://localhost:3000`.
# samplenodejsrepo